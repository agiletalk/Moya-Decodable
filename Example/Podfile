use_frameworks!

target 'Moya-Decodable_Example' do
  pod 'Moya-Decodable', :path => '../'

  target 'Moya-Decodable_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'
  end
end
